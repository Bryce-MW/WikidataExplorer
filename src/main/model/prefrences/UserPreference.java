package model.prefrences;

public interface UserPreference {
    /*
     * Class Description:
     * I never ended up implementing any preferences so this currently does nothing. I think that setting the preferred
     *  language(s) would have been one of the preferences but again, I never implemented any.
     */

    // I don't really know what kinds of preferences I will have quite yet.
}
